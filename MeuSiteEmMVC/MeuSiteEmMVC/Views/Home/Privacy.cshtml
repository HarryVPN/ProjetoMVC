@{
	Layout = "_Layout";
}

<head>
	<script src="https://cdn.socket.io/4.7.2/socket.io.min.js"></script>
</head>
<body class="row">

	<div class="card shadow">
		<a>Entrada da Guarita</a>

		<button id="btnLimpar">Limpar</button>

		<div class="form-group">
			<label for="nome">Nome</label>
			<input id="placaUi" type="text" class="form-control" aria-describedby="nomeHelp" placeholder="ABC1D23">
		</div>

		<div id="plates"></div>

		<img src="http://localhost:5000/video_feed">

	</div>
</body>

<script>
	const btnLimpar = document.getElementById('btnLimpar')
	const placaUi = document.getElementById('placaUi')

	const url = "http:localhost:5000"

	btnLimpar.addEventListener('click',async () => {
		const response = await fetch(url+"/reset", {
			method: 'GET',
			headers: {
				'Accept': 'application/json'
			}
		});

		btnLimpar.textContent = response
	})

	// async function atualizarPlaca() {
 //        try {
 //            const response = await fetch(url+"/get_placa")

 //            const data = response await response.json()

 //            console.log(data)

 //            if (data) {

 //                placaUi.value = data

 //                div.innerHTML = `
 //                    <p>
 //                        Plate: ${data}
 //                        Confidence: ${data}%
 //                    </p>
 //                `
 //            }
 //        } catch(err) {
 //            console.error(err)
 //        }
 //    }

 //    setInterval(atualizarPlaca, 1)

			// const socket = io("httplocalhost:5000");

			// socket.on('connect', () => {
			// 	console.log("Connected to Flask socket server")
			// });

			// socket.on('nova_placa', function(data) {
			// 	console.log(data)
				
			// 	placaUi.value = data.placa
				
			// 	placaUi.placeholder = data.placa+", "+data.confidence+"%"
				
			// 	const div = document.getElementById("plates");
			// 	div.innerHTML += `
			// 		<p>
			// 			Plate: ${data.placa}
			// 			Confidence: ${data.confidence}%
			// 		</p>
			// 	`;
			// });
	</script>